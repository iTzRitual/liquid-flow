// Block art (ASCII) for the Liquid Flow logo. The color is applied procedurally
// in components/Banner.jsx (a rainbow gradient). Here we keep only the shape —
// swapping the logo means editing this array.

export const ART = [
  '   ▄████████▄',
  '  ▄███▀▀  ▀▀████▄',
  ' ███▀        ▀███',
  '███            ██',
  '▀██   ▄███▄    █▀',
  ' ▀███████▀',
];

// The tagline is bilingual and lives in `translations.js` as the `Tagline` key
// (PL/EN) — reach for `t.Tagline`, do not hardcode text here.
